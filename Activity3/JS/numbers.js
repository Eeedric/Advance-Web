console.log("Numbers can be written with or without decimals:")

let j = 5.15;    // A number with decimals
let m = 9;       // A number without decimals
document.getElementById("numbers").innerHTML = j + "<br>" + m;